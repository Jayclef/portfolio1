import profile from "./profile-schema";
import job from "./jobs-schemas";
import project from "./project-schemas";
import contact from "./contact-schema";

const schemas = [profile, job, project, contact];

export default schemas;
